# RegNetAgents - Core Dependencies
# Gene regulatory network analysis with multi-agent AI
# Python 3.10+ required

# Multi-agent workflow orchestration
langgraph==1.0.7

# Model Context Protocol (Claude Desktop integration)
mcp==1.25.0

# Network analysis and centrality metrics
networkx==3.6.1

# Data manipulation
pandas==3.0.0
numpy==2.4.1

# HTTP requests (Reactome API, Ensembl API)
requests==2.32.5

# Route TLS verification through the OS trust store so HTTPS calls work
# behind an institutional TLS-inspecting proxy (Zscaler, Netskope, Norton, ...)
truststore==0.10.4

# Environment configuration
python-dotenv==1.2.1

# Statistical tests (Fisher's exact test for master regulator analysis)
scipy==1.17.1

# Optional: Ollama Python SDK (only needed for LLM-powered mode)
# pip install ollama>=0.1.0
# To enable: install from https://ollama.com/download, then: ollama pull llama3.1:8b
# Rule-based mode (default) works without this package.
