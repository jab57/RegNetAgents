# RegNetAgents - Core Dependencies
# Gene regulatory network analysis with multi-agent AI
# Python 3.10+ required

# Multi-agent workflow orchestration
langgraph>=0.2.0

# Model Context Protocol (Claude Desktop integration)
mcp>=1.0.0

# Network analysis and centrality metrics
networkx>=3.0

# Data manipulation
pandas>=2.0.0
numpy>=1.24.0

# HTTP requests (Reactome API, Ensembl API)
requests>=2.31.0

# Environment configuration
python-dotenv>=1.0.0

# Statistical tests (Fisher's exact test for master regulator analysis)
scipy>=1.10.0

# Ollama Python SDK (required for import; Ollama application is optional)
# Without the Ollama app running, system automatically uses rule-based fallback
# To enable LLM agents: install from https://ollama.com/download, then: ollama pull llama3.1:8b
ollama>=0.1.0
