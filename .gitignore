# Python packaging artifacts
*.egg-info/
dist/
build/

# Python virtual environments (common names)
regnetagents-env/
env/
venv/
.venv/
ENV/
VENV/

# Bytecode and cache
__pycache__/
*.py[cod]
cache/*.tmp
# Note: cache/gene_id_cache.pkl IS tracked (provides faster startup for users)

# OS-specific
.DS_Store
Thumbs.db

# IDE/project files
.vscode/
*.sublime-project
*.sublime-workspace

# Environment variables (SENSITIVE - never commit!)
.env

# GREmLN source (separate repository)
gremln_source/

# Results and outputs (users generate their own)
results/*.json
*_results.json

# Temporary files
*.tmp
*.log
.pytest_cache/
*.pyc

# Git/GitHub specific
.git/
.gitattributes

# Internal planning docs
RESEARCH_PAPER_PLAN.md
data/

# Private companion repo (jab57/RegNetAgents-private) - manuscript, planning
# docs, and held-back analysis. Tracked in a separate git repo under private/.
private/

# Local scratch / build artifacts (never part of the repo)
node_modules/
package.json
package-lock.json
poster.*
*.pptx
paper_preview.pdf